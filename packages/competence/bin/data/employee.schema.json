{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ti-engine.dev/schemas/competence/employee.json",
  "title": "Employee Profile",
  "description": "A single employee record containing personal and professional details.",
  "type": "object",
  "required": [
    "employeeID",
    "personal"
  ],
  "additionalProperties": false,
  "properties": {
    "employeeID": {
      "type": "string",
      "minLength": 1,
      "description": "Unique identifier for the employee"
    },
    "personal": {
      "type": "object",
      "description": "Personal information about the employee",
      "required": [
        "name",
        "level",
        "stage"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Full name of the employee"
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "Corporate email address"
        },
        "position": {
          "type": "string",
          "description": "Job title or position"
        },
        "organizationUnitID": {
          "type": "string",
          "description": "Organization unit ID"
        },
        "level": {
          "type": "string",
          "enum": [
            "N",
            "J",
            "R",
            "S",
            "X",
            "T"
          ],
          "description": "Competency level: N (Novice), J (Junior), R (Regular), S (Senior), X (Expert), T (Team Leader)"
        },
        "stage": {
          "type": "integer",
          "minimum": 1,
          "maximum": 3,
          "description": "Progression stage within the level (1-3)"
        },
        "startingDate": {
          "type": "string",
          "format": "date",
          "description": "Date of joining the company"
        }
      }
    }
  }
}
