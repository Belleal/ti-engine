{
  "//": [
    "Generates the published type declarations from the JSDoc already in the sources — same shape as",
    "the core package's build. Nothing is compiled: `emitDeclarationOnly` means the .js ships exactly",
    "as written and only .d.ts files are produced, and `checkJs` stays off deliberately, because this",
    "build exists to describe the engine to consumers rather than to type-check it.",
    "`static/` is excluded: it is a browser asset served to a page, not part of the module API.",
    "Run with `npm run build:types -w @ti-engine/web-content`. The output is committed, because the",
    "publish job deliberately runs no install and no lifecycle scripts."
  ],
  "compilerOptions": {
    "allowJs": true,
    "checkJs": false,
    "declaration": true,
    "emitDeclarationOnly": true,
    "module": "node16",
    "moduleResolution": "node16",
    "target": "es2022",
    "rootDir": ".",
    "outDir": "types",
    "skipLibCheck": true
  },
  "include": [
    "content/**/*.js",
    "render/**/*.js",
    "routes/**/*.js",
    "capture/**/*.js"
  ]
}
